[package]
name = "long_running_task"
version = "0.1.0"
edition = "2021"

[dependencies]
uuid = { version = "1.8.0", features = ["v4"] }
serde = { version = "1.0", features = ["derive"], optional = true }

[features]
default = ["lifespan"]
lifespan = []
serde = ["dep:serde"]
